# OptiStaff API
# Smart Dynamic Scheduler (Schedule Builder)

Install packages:
`npm install express cors body-parser mongodb mongoose`

To run the server:
node index.js

To stop the server:
ctrl + c
